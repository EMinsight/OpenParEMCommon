Useful scripts

checkErrors.sh - Lists error message numbers to identify missing numbers and to find the maximum assigned number.

proj_search - Recursively searches for setup files that either have or are missing keywords.  Install in $HOME/bin.

field_plot.py - Automates plot setup in ParaView.  Install in $HOME/.config/ParaView/Macros.

